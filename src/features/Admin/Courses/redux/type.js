const sliceName = "course";

const actions = {
  SET_COURSE_LIST: `${sliceName}/SET_COURSE_LIST`,
  SET_USER_ACCOUNT: `${sliceName}/SET_USER_ACCOUNT`,
  SET_CATEGORY_COURSE: `${sliceName}/SET_CATEGORY_COURSE`,
  SET_COURSE_DETAIL: `${sliceName}/SET_COURSE_DETAIL`,
};

export default actions;
